% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytics.R
\name{predict_engagement}
\alias{predict_engagement}
\title{Predict engagement rate using the ML microservice (MLP neural network)}
\usage{
predict_engagement(
  profiles,
  ml_url = Sys.getenv("RINSTAGRAM_ML_URL", "http://localhost:8001")
)
}
\arguments{
\item{profiles}{A data frame with at minimum \code{username},
\code{follower_count}, \code{following_count}, \code{posts_count}.
Optionally include \code{avg_likes} and \code{avg_comments} (from
\code{analyze()}) for higher accuracy.}

\item{ml_url}{Base URL of the ML service. Defaults to the
\code{RINSTAGRAM_ML_URL} environment variable, falling back to
\code{http://localhost:8001}.}
}
\value{
\code{profiles} with an added \code{ml_predicted_engagement} column.
}
\description{
Sends profile features to the rinstagram ML service and returns predicted
engagement rates from the trained feed-forward neural network.
}
\examples{
\dontrun{
  data <- analyze(scrape(c("cristiano")))
  data <- predict_engagement(data)
}
}
