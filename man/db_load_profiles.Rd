% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{db_load_profiles}
\alias{db_load_profiles}
\title{Load the most recent scraped profile(s) from the database}
\usage{
db_load_profiles(con, usernames = NULL)
}
\arguments{
\item{con}{A DBI connection from \code{db_connect()}.}

\item{usernames}{Optional character vector of usernames to filter by.
If \code{NULL} (default), all profiles are returned.}
}
\value{
A data frame of profiles (one row per username, most recent scrape).
}
\description{
Returns the most recently scraped record for each username in the
\code{profiles} table, using a window function to deduplicate by recency.
}
\examples{
\dontrun{
  con <- db_connect()
  all_profiles <- db_load_profiles(con)
  specific     <- db_load_profiles(con, c("cristiano", "leomessi"))
  DBI::dbDisconnect(con)
}
}
